{
  "name": "fsvr",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "fsvr": "./src/cli.ts"
  },
  "scripts": {
    "prepare:web": "bun run scripts/prepare-web.ts",
    "dev": "bun run prepare:web && bun run src/cli.ts",
    "build": "bun run scripts/build.ts",
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "citty": "^0.2.2",
    "fast-xml-parser": "^5.3.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "catalog:"
  }
}
